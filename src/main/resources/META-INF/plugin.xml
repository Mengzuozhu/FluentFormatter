<idea-plugin>
    <id>com.mengzz.FluentFormatter</id>
    <name>Fluent Formatter</name>
    <vendor email="863561164@qq.com" url="https://github.com/Mengzuozhu/FluentFormatter">zuozhu.meng</vendor>

    <description><![CDATA[

    <p><a href="https://github.com/Mengzuozhu/FluentFormatter">GitHub</a></p>
    <p>Formats the Java code in a fluent style, and generates the chained methods for the Java builder.</p>
    <br>

    <p><b>Features</b></p>
    <p> ● Wrap the invocation of each chained method on a separate line    </p>
    <p> ● Generate the all chained methods for the builder    </p>
    <br>

    <p><b>Example</b></p>
    <p>Before</p>
    <code>IntStream.range(1, 2).map(i -> i + 1).count();</code><br>

    <p>After formatted</p>
    <code>IntStream.range(1, 2)<br>
    &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;.map(i -> i + 1)<br>
    &nbsp;&nbsp;&nbsp;&nbsp;&nbsp;.count();
    </code><br>

    <br>
    <p><b>Usage</b></p>
    <p>Fluent format </p>
    <p>Select the code -> press <b>Alt+Enter</b> -> select <b>Fluent format</b></p>

    <br>
    <p>Fluent build </p>
    <p>Cursor at the code of Java builder -> press <b>Alt+Enter</b> -> select <b>Fluent build</b></p>

    ]]></description>

    <idea-version since-build="182"/>

    <depends>com.intellij.modules.java</depends>

    <extensions defaultExtensionNs="com.intellij">
        <intentionAction>
            <className>com.mengzz.fluentformatter.FluentFormatterAction</className>
        </intentionAction>
        <intentionAction>
            <className>com.mengzz.fluentformatter.FluentBuilderAction</className>
        </intentionAction>
    </extensions>

    <actions>
    </actions>
</idea-plugin>
